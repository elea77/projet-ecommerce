{% extends 'base.html.twig' %}

{% block title %}ReaGames - contact{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('/css/contact.css') }}">
{% endblock %}

{% block body %}

    <div class="container">
        <h1>Nous contacter</h1>
        {{ form(contactForm) }}

        {% for message in app.flashes('message') %}
            <div class="alert">
                {{ message }}
            </div>
        {% endfor %}
    </div>



{% endblock %}